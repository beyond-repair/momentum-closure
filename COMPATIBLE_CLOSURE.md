# Momentum closure — physics-compatible end state

$$
F_{\rm surface}=\oint T_{\rm eff}\cdot n\,dA
$$

is a valid force **only** when the complementary far-field (or channel) flux cancels it:

$$
F_d+F_X=0.
$$

Closed-box nonzero net from Ware×G under free static maps: **extinguished** (Stage-2).

See coherence-drive `PHYSICS_COMPATIBLE_CLOSURE.md`.
